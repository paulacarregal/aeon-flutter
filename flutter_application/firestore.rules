rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // TEMPORÁRIO:
    // Usuários autenticados podem ler e escrever no Firestore.
    // Será substituído por regras específicas posteriormente.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}